# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Environment

- Python 3.9 (CPython) with a local virtualenv at `venv/`
- Activate: `source venv/bin/activate`
- Run the script: `python main.py`
- Install dependencies: `pip install <package>`

## Project

`artistForceMap` is a nascent Python project (PyCharm-generated scaffold). The only source file is `main.py`. No tests, linter, or build tooling have been configured yet.